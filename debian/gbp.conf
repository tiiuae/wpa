[DEFAULT]
debian-branch=debian/experimental
upstream-branch=upstream/experimental

[import-orig]
postimport = dch -v%(version)s New upstream release.; git add debian/changelog; debcommit -m "Prepare changelog entry"
